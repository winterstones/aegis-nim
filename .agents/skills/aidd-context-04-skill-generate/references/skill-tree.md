# Skill tree

A generated skill has this shape:

```text
<skill-name>/
├── SKILL.md
├── actions/
│   └── <NN>-<action>.md
├── references/
│   └── <reference>.md
└── assets/
    └── <asset>
```

Omit `actions/`, `references/`, or `assets/` when empty.
