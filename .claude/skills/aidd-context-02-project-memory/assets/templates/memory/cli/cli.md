# CLI

The command-line tool: its commands, inputs, and distribution.

## Commands

- <The main commands or subcommands and what each does>

## Interface

- <Global flags, input and output conventions, exit codes>

## Distribution

- <How it is installed (npm bin, brew, binary), the entry point>

<!--
Capture: the command surface, the I/O conventions, the distribution.
Skip: every flag. Point to the help output. Remove this comment when filled.
-->
