# API

The HTTP API surface: its style, the main resources, and the contracts.

## Style

- <Server/RPC surface: REST, GraphQL, or RPC, the framework, where server routes are defined>
- <Versioning and base path>

## Resources

- <The main resource or endpoint groups, one line each>

## Contracts

- <Request and response conventions, error format, pagination>
- <Where the spec lives (OpenAPI, schema)>

<!--
Capture: the API style, the macro resource list, the contract conventions.
Skip: every endpoint. Point to the spec. Remove this comment when filled.
-->
